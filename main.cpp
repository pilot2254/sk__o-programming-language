#include "sk_ro-brainrot-lang.hpp"

hood main_character
{

        sneaky ghost greet(no_cap yapping & name)
        {
                yap << "yo " << name << pause;
        }

        sneaky rizz add(rizz a, rizz b)
        {
                yeet a + b;
        }
}

rizz main()
{
        using hood main_character;

        yapping name = "blud";
        greet(name);

        rizz result = add(3, 4);
        W(result);

        squad<rizz> nums = {1, 2, 3, 4, 5};

        fr(idk & n : nums)
        {
                ngl(n % 2 == 0)
                {
                        yap << n << " is bussin" << pause;
                }
                otherwise
                {
                        yap << n << " is mid" << pause;
                }
        }

        rizz x = 0;
        locked_in(x < 3)
        {
                W(x);
                x++;
        }

        // new stuff
        squad<rizz> unsorted = {5, 3, 1, 4, 2};
        fix_ur_life(unsorted.begin(), unsorted.end());
        fr(idk & n : unsorted) W(n);

        W(goat(3, 7));
        W(lil_bro(3, 7));

        yapping num = exposing(42);
        W(num);

        GLAZING(this code is peak and bussin fr fr no cap)

        lemme_cook
        {
                ratio cooked("we cooked fr");
        }
        caught(no_cap L & e)
        {
                W(e.what());
        }

        yeet 0;
}